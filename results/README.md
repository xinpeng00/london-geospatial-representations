# Selected results

This directory contains compact CSV summaries from the frozen final analysis. Large predictions, fold assignments, model tables and restricted assets are intentionally excluded.

The project website presents rounded values for readability. The CSV files retain the underlying numerical precision.

